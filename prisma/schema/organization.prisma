model Organization {
  id        Int      @id @default(autoincrement())
  name      String
  slug      String   @unique
  users     User[]
  orders    Order[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
