meta {
  name: 3.1 Login para obter tokens frescos
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{SUPER_ADMIN_EMAIL}}",
    "password": "{{SUPER_ADMIN_PASSWORD}}"
  }
}
