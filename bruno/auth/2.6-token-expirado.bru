meta {
  name: 2.6 Token expirado → 400
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/auth/verify-email
  body: json
  auth: none
}

body:json {
  {
    "token": "token-expirado-qualquer"
  }
}
