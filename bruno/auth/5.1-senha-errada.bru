meta {
  name: 5.1 Login com senha errada → 401 INVALID_CREDENTIALS
  type: http
  seq: 22
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{SUPER_ADMIN_EMAIL}}",
    "password": "senha-errada"
  }
}
